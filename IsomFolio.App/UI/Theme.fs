module IsomFolio.UI.Theme

open Avalonia.Media

// Backgrounds
let mainBg          = Color.Parse("#252526")
let panelBg         = Color.Parse("#1E1E1E")
let scanBarBg       = Color.Parse("#1E3A5F")

// Interactive surfaces
let tileBg          = Color.Parse("#2D2D2D")
let surfaceBg       = Color.Parse("#3D3D3D")
let tagChipBg       = Color.Parse("#333333")

// Accent / selection
let accent          = Color.Parse("#0078D4")

// Text
let textMuted       = Color.Parse("#888888")
let textDim         = Color.Parse("#AAAAAA")

// Status
let warningBg       = Color.Parse("#9D5100")
let errorBg         = Color.Parse("#C42B1C")

// Folder tree (sidebar selection state)
let folderSelectedText      = Color.Parse("#FFFFFF")
let folderUnselectedText    = Color.Parse("#CFCFCF")
let folderSelectedPath      = Color.Parse("#D6ECFF")
let folderUnselectedPath    = Color.Parse("#7F7F7F")
let folderSelectedBg        = Color.Parse("#0B3D62")
let folderSelectedBorder    = Color.Parse("#8FD3FF")

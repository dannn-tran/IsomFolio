{
  "name": "faces",
  "version": "1.0.0",
  "capabilities": ["inference_engine"],
  "needs_setup": false,
  "description": "Local face detection and recognition (InsightFace ONNX). Runs as an HTTP inference engine; the host clusters faces into people.",
  "config_schema": [
    {
      "key": "model_variant",
      "label": "Model size",
      "kind": "select",
      "options": ["buffalo_l", "buffalo_s"],
      "default": "buffalo_l",
      "description": "buffalo_l: high accuracy, ~280 MB download. buffalo_s: smaller and faster, ~25 MB."
    }
  ]
}

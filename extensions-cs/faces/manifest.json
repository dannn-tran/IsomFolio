{
  "name": "faces",
  "version": "1.0.0",
  "capabilities": ["cluster_faces"],
  "needs_setup": true,
  "description": "Local face detection and clustering using InsightFace ONNX models",
  "config_schema": [
    {
      "key": "eps",
      "label": "Cluster sensitivity",
      "kind": "number",
      "default": "0.4",
      "min": 0.1,
      "max": 2.0,
      "description": "How similar faces must be to group together. Lower = stricter matching."
    },
    {
      "key": "min_pts",
      "label": "Min faces per cluster",
      "kind": "integer",
      "default": "2",
      "min": 1.0,
      "description": "Minimum number of faces required to form a cluster."
    }
  ]
}

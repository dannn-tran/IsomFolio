{
  "name": "autotag-clip",
  "version": "1.0.0",
  "capabilities": ["classify"],
  "has_install_step": true,
  "description": "Auto-tag photos using CLIP (local inference, offline after first download)",
  "config_schema": [
    {
      "key": "variant",
      "label": "Model",
      "kind": "select",
      "options": ["clip-vit-b32", "clip-vit-l14"],
      "default": "clip-vit-b32"
    },
    {
      "key": "vocabulary_file",
      "label": "Vocabulary file (one tag per line, blank = built-in 49 tags)",
      "kind": "text"
    },
    {
      "key": "batch_size",
      "label": "Inference batch size (auto = detect from CPU cores)",
      "kind": "text",
      "default": "auto"
    }
  ]
}
